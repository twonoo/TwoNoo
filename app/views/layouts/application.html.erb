<!DOCTYPE html>
<!--[if lt IE 7 ]><html class="ie ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]><html class="ie ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]><html class="ie ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!-->
<html lang="en">
<!--<![endif]-->
<head profile="http://www.w3.org/2005/10/profile">
<link rel="icon" 
      type="image/png" 
      href="<%=asset_url 'TwoNoo-Logo-compact3.png'%>">
	<!-- Basic Page Needs ================================================== -->
  <meta charset="utf-8">
  <meta content="IE=edge" http-equiv="X-UA-Compatible">
  <meta name="google-site-verification" content="EiVIH4ce5RGmJBuZIatvMd1DBQyRsmuMOvQRncxitIM" />
  <meta name="description" content="Find friends and things to do. Meet people and do stuff. Create and Find activities and events.">
	<meta name="author" content="TwoNoo">

<% if !@activity.nil? && !@activity.id.nil? %>
  <meta property="og:url" content="<%=activity_url(:id => @activity.id)%>" />
  <meta property="og:title" content="<%= @activity.activity_name %>" />
  <meta property="og:description" content="<%= @activity.description %>" />
  <% if @activity.image.exists? %>
    <meta property="og:image" content="<%=root_url%><%= @activity.image.url %>" />
  <% else %>
    <meta property="og:image" content="<%= asset_url 'TwoNoo-Logo.png' %>" />
  <% end %>
<% else %>
  <% if user_signed_in?%>
    <meta property="og:url" content="<%=new_user_registration_url(:referrer => current_user.id)%>" />
  <% else %>
    <meta property="og:url" content="<%=root_url%>" />
  <% end %>
  <meta property="og:description" itemprop="description" content="Find friends and things to do. Meet people and do stuff. Create and Find activities and events." />
  <meta property="og:title" content="TwoNoo - Meet People. Do Stuff." />
  <meta property="og:image" content="<%= asset_url 'TwoNoo-Logo.png' %>" />
<% end %>

  <%= csrf_meta_tags %>
  <title><%= @title ? @title : 'TwoNoo - Meet People. Do Stuff.' %></title>

	<!-- Mobile Specific Metas ================================================== -->
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

	<!-- Stylesheets ================================================== -->
  <%= stylesheet_link_tag 'application', media: 'all' %>

  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">

  <!--[if lt IE 9]><script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script> <![endif]-->

  <!-- web font  -->
	<link href="http://fonts.googleapis.com/css?family=Open+Sans:300,400,600,800" rel="stylesheet" type="text/css">

  <%= javascript_include_tag 'application' %>
  <script type="text/javascript" src="https://apis.google.com/js/plusone.js"></script>
  <script>
  $(document).ready(function() {
    $(function() {
      console.log('getting google stuff');
      var script = document.createElement('script');
      script.type = 'text/javascript';
      script.src = 'https://maps.googleapis.com/maps/api/js?v=3.exp&' +
          'callback=initialize';
      document.body.appendChild(script);
      console.log('done getting google stuff');
    }); 
    
    (function(d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0];
      if (d.getElementById(id)) return;
      js = d.createElement(s); js.id = id;
      js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&appId=1524267501137363&version=v2.0";
      fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'facebook-jssdk'));

    $(function(){
      $('.navbar-toggle').click(function(){
        var buttons = $('navbar-toggle');
        console.log("buttons", buttons);
        console.log("buttons", $(this).get(0));

        $(this).siblings('.navbar-toggle:not(.collapsed)').click();
      });

    });
  });
  </script>
</head>
	<body>

<div id="fb-root"></div>

		<!-- header -->
		<header id="mainHeader" class="navbar-fixed-top" role="banner">
			<div class="container-fluid">
              <ul id="mobile-nav" class="navbar-toggle collapsed">
                <li id="my-stuff">
                  <% if current_user.present? %>
                      <a href="<%= user_activities_path(current_user) %>">
                  <% else %>
                      <a href="<%= user_session_path %>">
                  <% end %>
                  <span class="glyphicon glyphicon-user"></span>MY STUFF
                  <% if num_alerts > 0 %>
                      <span class="counter"><%= num_alerts > 99 ? "*" : num_alerts %></span>
                  <% end %>
                </a>
                </li>

                <li id="create"><a href="<%= activities_new_path %>"><span class="icon glyphicon glyphicon-plus"></span>CREATE
                </a></li>

                <li id="activities"><a href="<%= root_path %>"><span class="icon fa fa-users"></span>ACTIVITIES
                </a></li>

                <li id="messages"><a href="<%= conversations_path %>"><span class="icon glyphicon glyphicon-envelope"></span>MESSAGES
                  <% if num_inbox > 0 %>
                      <span class="counter"><%= num_inbox > 99 ? "*" : num_inbox %></span>
                  <% end %>
                </a></li>

                <li id="alert"><a href="<%= alerts_path %>"><span class="icon fa fa-bell"></span>ALERTS
                  <% if num_notifications > 0 %>
                    <span class="counter"><%= num_notifications > 99 ? "*" : num_notifications %></span>
                  <% end %>
                </a></li>

                <li id="menu"><a href="#" data-toggle="collapse" data-target=".navbar-ex1-collapse"><span class="fa fa-bars"></span></a></li>
              </ul>
              <% unless current_user.present? %>
                  <ul id="mobile-signup-signin" class="navbar-toggle collapsed">
                    <li id="sign-up"><a href="<%= new_user_registration_path %>">SIGN UP</a></li>
                    <li id="sign-in"><a href="<%= user_session_path %>">SIGN IN</a></li>
                  </ul>
              <% end %>
                  <nav class="navbar navbar-default scrollMenu" role="navigation">
					<div class="navbar-header">
                      <!--<button id="main_menu" type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-ex1-collapse" style="width:40px;height:34px;"> <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </button>-->
              <!--<% if current_user.present? %>-->
                <!--<button id="notifications_mobile" type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-ex2-collapse" style="font-size:.8em;width:40px;height:34px;padding:0px;vertical-align:middle;"> <span class="sr-only">Toggle navigation</span><span class="glyphicon glyphicon-exclamation-sign"></span><span class="badge" id="notifications_badge_mobile" style="font-size:.5em;"><%=current_user.mailbox.notifications({:read => false}).count%></span></button>-->
                <!--<button id="messages_mobile" type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-ex3-collapse" style="font-size:.8em;width:40px;height:34px;padding:0px;vertical-align:middle;"> <span class="sr-only">Toggle navigation</span><span class="glyphicon-envelope"></span><span class="badge" id="messages_badge_mobile" style="font-size:.5em;"><%=current_user.mailbox.inbox({:read => false}).count%></span></button>-->
              <!--<% end %>-->
						<a class="collapse navbar-collapse navbar-brand" href="/"><img class="logo" height="35px" src="<%= asset_path 'main-logo.png' %>"></a>
          </div>
          <div class="collapse navbar-collapse navbar-ex1-collapse" id="scrollTarget" style="height: 1px;">
            <%= current_user ? render('layouts/signed_in_nav') : render('layouts/sign_in_nav') %>
          </div>
          <% if current_user.present? %>
            <%= render partial: '/notifications/notifications_mobile' %>
          <% end %>
          <% if current_user.present? %>
            <%= render partial: '/messages/messages_mobile' %>
          <% end %>
        </nav>
      </div>
    </header>
				<!-- header -->
    <% unless current_user.present? %>
        <div id="mobile-signup-signin-space" class="navbar-toggle collapsed">
          &nbsp
        </div>
    <% end %>

    <%= form_tag("/search", method: "get", id: "search_form", onsubmit: "return confirmSearch();") do %>
    <div class="mobile-search navbar-toggle collapsed" style="padding: 10px; height: 140px; float: none; margin: 0">
        <div class="" style="padding-bottom: 10px">
          <input style="border: 1px solid;" class="form-control" id="terms" name="terms" placeholder="Find activities/events...blank shows all" type="search" title="Find activities/events...blank shows all">
          <span class="glyphicon glyphicon-search" aria-hidden="true" style="position: absolute; top: 72px; right: 22px;"></span>
        </div>

        <div class="btn-group" id="location-selection-mobile" style="padding-bottom: 10px; width: 49%; float: left;">
          <input type="hidden" id="lat" name="lat" value="39.737567"></input>
          <input type="hidden" id="lng" name="lng" value="-104.9847179"></input>
          <input type="text" id="location" name="location" class="form-control input-md" style="width:125px;display:none;"
                 <% if user_signed_in? && !(current_user.profile.city.nil? || current_user.profile.state.nil?) %>
                 value="<%=current_user.profile.city%>, <%=current_user.profile.state%>"
                 <% elsif !(cookies["city"].nil? || cookies["state"].nil?) %>
                 value="<%=cookies["city"]%>, <%=cookies["state"]%>"
                 <% else %>
                 value="Denver, CO"
                 <% end %>
                 placeholder="City or ZIP">
          <a id="selection" class="selection location_selection_mobile" style="color: inherit;">
            <% if user_signed_in? && !(current_user.profile.city.nil? || current_user.profile.state.nil?) %>
                <%=current_user.profile.city%>, <%=current_user.profile.state%>
            <% elsif !(cookies["city"].nil? || cookies["state"].nil?) %>
                <%=cookies["city"]%>, <%=cookies["state"]%>
            <% else %>
                Denver, CO
            <% end %>
            <i class='glyphicon glyphicon-chevron-down'></i>
          </a>
        </div>

        <div class="" style="padding-bottom: 10px;width: 49%; float: left;">
          <div class="btn-group" id="date-dropdown">
            <a style="cursor: pointer; background-color: inherit; border: none; color: inherit;" class="dropdown-toggle search_criteria" data-toggle="dropdown" >
            <input type="hidden" name="type" class="hidden-input" id="type-selection" value="All">
            <span class="selection" id="activitytype-selection-phrase" sytle="">All</span> <i class='glyphicon glyphicon-chevron-down'></i></a>
            <ul class="dropdown-menu" role="menu">
              <li><a href="#">All</a></li>
              <% ActivityType.all.each do |at| %>
                  <li><a href="#"><%=at.activity_type%></a></li>
              <% end %>
            </ul>
          </div>
        </div>
    </div>

        <input class="btn" name="commit" type="submit" value="Search" style="display: none">
    <% end %>

    <div style="padding-bottom: 50px;">
        <%= flash_messages %>
        <%= yield %>
      </div>
      <%= render 'layouts/footer' %>

    <%= javascript_include_tag "welcome" %>

    <script>
	   (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
	   (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
	   m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
	   })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
	 
	   ga('create', 'UA-55676731-1', 'auto');
	   ga('send', 'pageview');
     <% if params[:terms] %>
     ga('send', 'pageview', '/search_results.php?q=<%=params[:terms]%>');
     <% end %>
	 </script>
	</body>
</html>
