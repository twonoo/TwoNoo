<ul class="nav navbar-nav pull-right navbar-toggle collapsed" style="padding-top: 0; margin-top: 0;">
  <li><%= link_to 'Home', root_path %></li>
  <li class="divider"></li>
  <li class=""><%= link_to 'My Profile', current_user ? profile_path(current_user) : new_user_session_path(flash_alert: 'You must be logged in to do that.') %></li>
  <li class=""><%= link_to 'Settings', profile_edit_path %></li>
  <% if current_user %>
      <li>
      <% if current_user && current_user.recommended_followers.count > 0 %>
              <%= link_to ('People You Know <span class="red_badge">' + current_user.recommended_followers.count.to_s+ '</span>').html_safe, people_path %>
            <% else %>
              <%= link_to ('People You Know <span class="badge">0</span>').html_safe, people_path %>
            <% end %>
      </li>
  <% end %>
  <li>
    <a href="/credits/purchase">Credits
      <span class="badge"><%= Transaction.get_balance(current_user) if current_user %></span>
    </a>
  </li>
  <li><%= link_to "Feedback", '#', class: 'feedback' %></li>
  <% if current_user %>
      <li class=""><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %> </li>
  <% else %>
      <li class=""><%= link_to 'Sign In', new_user_session_path %> </li>
  <% end %>
</ul>
